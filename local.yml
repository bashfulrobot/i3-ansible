- hosts: localhost
  become: true
  pre_tasks:
    - name: Update Repositories
      apt: update_cache=yes
      changed_when: false
  tasks:
    - include_vars:
      file: config.yml
    - include: tasks/user.yml
